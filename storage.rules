rules_version = '2';
// Cloud Storage is not used by the application. Deny everything until a
// feature needs it; then add narrowly scoped rules here.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
