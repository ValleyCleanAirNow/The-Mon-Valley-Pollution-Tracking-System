# Copy to frontend/.env and fill in from Firebase console > Project settings >
# Your apps > Web app config. These identify the Firebase project and are safe
# to ship in the browser bundle; access is controlled by Firestore rules.
# The API key is left blank on purpose: the repo's secret scanner flags any
# Google API key pattern. Copy it from the console into frontend/.env only.
REACT_APP_FIREBASE_API_KEY=
REACT_APP_FIREBASE_AUTH_DOMAIN=mv-pollution-tracking-system.firebaseapp.com
REACT_APP_FIREBASE_PROJECT_ID=mv-pollution-tracking-system
REACT_APP_FIREBASE_STORAGE_BUCKET=mv-pollution-tracking-system.firebasestorage.app
REACT_APP_FIREBASE_MESSAGING_SENDER_ID=245354192374
REACT_APP_FIREBASE_APP_ID=1:245354192374:web:14b7f86ad26b3232933bd7

# Web push (Firebase console > Project settings > Cloud Messaging > Web Push
# certificates > key pair). Leave empty to hide device notifications.
REACT_APP_FIREBASE_VAPID_KEY=

# Show the text-message channel in the Alerts screen. Must match the
# SMS_ALERTS_ENABLED functions parameter, otherwise people can subscribe to
# SMS that never sends.
REACT_APP_SMS_ALERTS_ENABLED=false

# Base URL of deployed Cloud Functions, used by BreatheAI.
REACT_APP_FUNCTIONS_BASE_URL=https://us-central1-mv-pollution-tracking-system.cloudfunctions.net

# Local development only: point the app at the Firebase Emulator Suite.
REACT_APP_USE_EMULATORS=false
REACT_APP_EMULATOR_HOST=127.0.0.1

# NOTE: There is intentionally no PurpleAir or OpenWeatherMap key here.
# Third-party data is fetched server-side by Cloud Functions; the client only
# reads Firestore. See functions/.env.example for server secrets.
